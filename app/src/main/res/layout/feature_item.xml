<?xml version="1.0" encoding="utf-8"?>

<FrameLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        >

    <LinearLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:minHeight="45dp"
            android:orientation="horizontal"
            >

        <TextView
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:id="@+id/feature_key"
                android:text="Категория"
                android:layout_weight="1"
                android:maxLines="2"

                android:textSize="@dimen/mobium_fragment_product_feature_item_text_size"

                android:gravity="center_vertical"
                android:textColor="@color/design_dark"
                android:paddingLeft="@dimen/mobium_fragment_product_share_buttons_layout_left_padding"

                android:paddingBottom="4dp"
                android:paddingTop="4dp"
                android:paddingRight="8dp"/>

        <TextView
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:id="@+id/feature_value"
                android:text="Смартфоны Android"
                android:layout_weight="1"
                android:maxLines="2"

                android:textSize="@dimen/mobium_fragment_product_feature_item_text_size"

                android:gravity="center_vertical"
                android:textColor="@color/design_dark"
                android:paddingRight="8dp"
                android:paddingLeft="@dimen/mobium_fragment_product_share_buttons_layout_left_padding"

                android:paddingBottom="4dp"
                android:paddingTop="4dp"/>
    </LinearLayout>

    <View
            android:layout_marginTop="?attr/actionBarSize"
            style="@style/deliver.horizontal"/>

</FrameLayout>
